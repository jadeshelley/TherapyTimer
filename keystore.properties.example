# Copy this file to keystore.properties and fill in your real values.
# keystore.properties is in .gitignore — do not commit it.
#
# Create the keystore first: Build → Generate Signed Bundle / APK → Create new...
# Then set storeFile to the full path to the .jks file (use forward slashes or escaped backslashes).

storePassword=YourKeystorePassword
keyPassword=YourKeyPassword
keyAlias=therapy-timer-key
storeFile=C:/path/to/your/therapy-timer-keystore.jks
